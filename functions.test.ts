import { isPalinDrome, isPrimeNumber,areAnagram,countWords,reverseString } from './functions';

test('isPalinDrome returns true for palindrome', () => {
  
});

test('isPalinDrome returns false for non-palindrome', () => {
  
});

test('isPrimeNumber returns true for prime number', () => {
  
});

test('isPrimeNumber returns false for non-prime number', () => {
  
});

test('areAnagram returns true for anagram', () => {
  
});

test('areAnagram returns false for non-anagram', () => {
  
});

test('countWords returns correct number of words', () => {
  
});

test('countWords returns correct number of words for empty string', () => {
  
});

test('countWords returns correct number of words for sentence with multiple spaces', () => {
  
});

test('reverseString returns correct reversed string', () => {
  
});